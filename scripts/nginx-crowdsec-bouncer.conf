# CrowdSec nginx bouncer — included from nginx http context via conf.d.
# The bouncer listens on 127.0.0.1:7443 and is queried by lua or auth_request.

map $remote_addr $crowdsec_bouncer_upstream {
    default 127.0.0.1:7443;
}
